// Brand fonts for the Remotion render, bundled OFFLINE (base64 @font-face) so the isolated render
// worker needs no network. Regenerate with: node tools/fetch-fonts.mjs
// (The web app loads the same families online via the <link> in index.html — unaffected.)
import "./fonts-local.css";
